/*
Copyright © 2024 NAME HERE <EMAIL ADDRESS>
*/
package cmd

/*
import (
	"fmt"

	"github.com/Jatin020403/BasaltDB/database"
	"github.com/spf13/cobra"
)

// getPartitionsCmd represents the getPartitions command
var getPartitionsCmd = &cobra.Command{
	Use:   "getPartitions",
	Short: "View all partitions",
	Long:  `View names of all partitions`,
	Run: func(cmd *cobra.Command, args []string) {
		ptns, err := database.GetAllPartitions()
		if err != nil {
			fmt.Println(err)
		}

		for _, pt := range ptns {
			fmt.Println(pt)
		}
	},
}

func init() {
	rootCmd.AddCommand(getPartitionsCmd)
}
*/
